"""GitView - a browser-based interactive git repository analyzer.

GitView reads the ``.git`` directory of a git-initialized project and serves
an interactive browser view of its commits, branches, remotes and tags,
including a graph/tree visualization of history.
"""

__version__ = "1.1.0"
